server:
  port: ${port:8090}

spring:
  application:
    name: eshop-eureka-service-01

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://root:root@127.0.0.1:8090/eureka/

security:
  basic:
    enabled: true
  user:
    name: root
    password: root
